// Atributo primário que define a identidade ofensiva da classe atual do jogador
// (qual dos três gera Attack Power / Spell Power). Ainda não existe um sistema de
// classes — por enquanto isso é só um campo em StatsManager, pronto para quando
// promoção de classe existir.
public enum PrimaryAttribute
{
    Strength,
    Agility,
    Intelligence
}
